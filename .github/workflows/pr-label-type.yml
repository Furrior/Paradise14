name: "Labels: PR"

on:
- pull_request_target

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    if: github.actor != 'PJBot' && github.actor != 'DeltaV-Bot'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5
